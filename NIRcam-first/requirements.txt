# requirements.txt
# Two-Band Filter 觸發系統依賴

# 核心依賴
numpy>=1.21.0
scipy>=1.7.0           # SimpleTracker 需要（匈牙利演算法）
opencv-python>=4.5.0

# YOLO（如果使用）
# ultralytics>=8.0.0   # 取消註釋以安裝 YOLOv8

# 可選依賴
# matplotlib>=3.3.0    # 用於繪圖和分析
# pandas>=1.3.0        # 用於資料分析

# 注意：
# - 如果已安裝 Anaconda，numpy 和 scipy 可能已經存在
# - opencv-python 可能需要手動安裝
# - ultralytics 只在使用 YOLO 模型時需要
